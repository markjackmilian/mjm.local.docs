<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\Mjm.LocalDocs.Core\Mjm.LocalDocs.Core.csproj" />
    <ProjectReference Include="..\Mjm.LocalDocs.Infrastructure\Mjm.LocalDocs.Infrastructure.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.2">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="ModelContextProtocol.AspNetCore" Version="0.6.0-preview.1" />
<PackageReference Include="Serilog.AspNetCore" Version="10.0.0" />
    <PackageReference Include="Serilog.Sinks.File" Version="7.0.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
<UserSecretsId>8ef649af-010c-4434-8adc-98da93e2e355</UserSecretsId>
  </PropertyGroup>

  <!-- React client build integration -->
  <Target Name="BuildReactApp" BeforeTargets="Build"
          Condition="!Exists('wwwroot/index.html') Or '$(Configuration)' == 'Release'">
    <Exec WorkingDirectory="client" Command="npm install" />
    <Exec WorkingDirectory="client" Command="npm run build" />
  </Target>

  <Target Name="PublishReactApp" BeforeTargets="Publish">
    <Exec WorkingDirectory="client" Command="npm install" />
    <Exec WorkingDirectory="client" Command="npm run build" />
  </Target>

</Project>
